import React from 'react';

import NewInput from 'components/NewInput';

const EmailInput = (props) => {
  // const onChange = (e) => {
  //   onChange && onChange(e);
  // };

  // const onKeyDown = (event) => {
  // };

  return <NewInput type='text' {...props} />;
};

export default EmailInput;
